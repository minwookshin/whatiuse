{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "column-manager",
  "title": "Column Manager",
  "description": "Column visibility, order, pinning, and width reset with accessible named controls.",
  "dependencies": [
    "@phosphor-icons/react@^2.1.10"
  ],
  "registryDependencies": [
    "@whatiuse/whatiuse-base",
    "@whatiuse/button",
    "@whatiuse/button-group",
    "@whatiuse/checkbox",
    "@whatiuse/icon-button",
    "@whatiuse/popover"
  ],
  "files": [
    {
      "path": "registry/components/ui/column-manager.tsx",
      "content": "\"use client\";\n\nimport \"../../styles/whatiuse-base.css\";\nimport \"../../styles/components/column-manager.css\";\nimport {\n  ArrowCounterClockwise,\n  ArrowDown,\n  ArrowUp,\n  CaretDown,\n  PushPin,\n  SlidersHorizontal,\n} from \"@phosphor-icons/react\";\nimport { Button } from \"./button\";\nimport { ButtonGroup } from \"./button-group\";\nimport { Checkbox } from \"./checkbox\";\nimport { IconButton } from \"./icon-button\";\nimport { Popover, PopoverContent, PopoverTitle, PopoverTrigger } from \"./popover\";\n\nexport type ColumnManagerPin = false | \"start\" | \"end\";\n\nexport type ColumnManagerColumn = {\n  id: string;\n  label: string;\n  visible: boolean;\n  required?: boolean;\n  pinned?: ColumnManagerPin;\n};\n\nexport type ColumnManagerProps = {\n  columns: readonly ColumnManagerColumn[];\n  onVisibilityChange: (id: string, visible: boolean) => void;\n  onOrderChange?: (orderedIds: readonly string[]) => void;\n  onPinningChange?: (id: string, pinned: ColumnManagerPin) => void;\n  onResetSizing?: () => void;\n  label?: string;\n};\n\nfunction nextPin(pin: ColumnManagerPin | undefined): ColumnManagerPin {\n  if (!pin) return \"start\";\n  if (pin === \"start\") return \"end\";\n  return false;\n}\n\nfunction pinLabel(column: ColumnManagerColumn): string {\n  if (column.pinned === \"start\") return `Pin ${column.label} to end`;\n  if (column.pinned === \"end\") return `Unpin ${column.label}`;\n  return `Pin ${column.label} to start`;\n}\n\nexport function ColumnManager({\n  columns,\n  onVisibilityChange,\n  onOrderChange,\n  onPinningChange,\n  onResetSizing,\n  label = \"Columns\",\n}: ColumnManagerProps) {\n  const visibleCount = columns.filter((column) => column.visible).length;\n  const move = (index: number, offset: -1 | 1) => {\n    const target = index + offset;\n    if (target < 0 || target >= columns.length) return;\n    const orderedIds = columns.map((column) => column.id);\n    [orderedIds[index], orderedIds[target]] = [orderedIds[target], orderedIds[index]];\n    onOrderChange?.(orderedIds);\n  };\n\n  return (\n    <Popover>\n      <PopoverTrigger\n        render={(\n          <Button\n            size=\"small\"\n            variant=\"ghost\"\n            leadingIcon={<SlidersHorizontal />}\n            trailingIcon={<CaretDown />}\n            aria-label={`${visibleCount} of ${columns.length} columns visible`}\n          />\n        )}\n      >\n        {label}\n      </PopoverTrigger>\n      <PopoverContent className=\"whatiuse-column-manager\" align=\"end\">\n        <div className=\"whatiuse-column-manager__header\">\n          <PopoverTitle className=\"whatiuse-column-manager__title\">Columns</PopoverTitle>\n          <span>{visibleCount} visible</span>\n        </div>\n        <ol className=\"whatiuse-column-manager__list\" aria-label=\"Table columns\">\n          {columns.map((column, index) => (\n            <li key={column.id} className=\"whatiuse-column-manager__row\" data-pinned={column.pinned || undefined}>\n              <Checkbox\n                label={column.label}\n                checked={column.visible}\n                disabled={column.required}\n                onCheckedChange={(checked) => onVisibilityChange(column.id, checked)}\n              />\n              <ButtonGroup aria-label={`${column.label} column actions`}>\n                {onPinningChange && (\n                  <IconButton\n                    size=\"small\"\n                    variant=\"ghost\"\n                    aria-label={pinLabel(column)}\n                    tooltip={pinLabel(column)}\n                    onClick={() => onPinningChange(column.id, nextPin(column.pinned))}\n                  >\n                    <PushPin aria-hidden=\"true\" weight={column.pinned ? \"fill\" : \"regular\"} />\n                  </IconButton>\n                )}\n                {onOrderChange && (\n                  <>\n                    <IconButton size=\"small\" variant=\"ghost\" aria-label={`Move ${column.label} up`} disabled={index === 0} onClick={() => move(index, -1)}>\n                      <ArrowUp aria-hidden=\"true\" />\n                    </IconButton>\n                    <IconButton size=\"small\" variant=\"ghost\" aria-label={`Move ${column.label} down`} disabled={index === columns.length - 1} onClick={() => move(index, 1)}>\n                      <ArrowDown aria-hidden=\"true\" />\n                    </IconButton>\n                  </>\n                )}\n              </ButtonGroup>\n            </li>\n          ))}\n        </ol>\n        {onResetSizing && (\n          <Button className=\"whatiuse-column-manager__reset\" size=\"small\" variant=\"quiet\" leadingIcon={<ArrowCounterClockwise />} onClick={onResetSizing}>\n            Reset widths\n          </Button>\n        )}\n      </PopoverContent>\n    </Popover>\n  );\n}\n",
      "type": "registry:component",
      "target": "components/ui/column-manager.tsx"
    },
    {
      "path": "registry/styles/components/column-manager.css",
      "content": "/* Generated from src/foundation.css and src/styles.css. Do not edit directly. */\n\n@layer whatiuse.tokens, whatiuse.base, whatiuse.components;\n\n@layer whatiuse.components {\n.whatiuse-column-manager {\n  width: min(316px, calc(100vw - 24px));\n  padding: 8px;\n}\n\n.whatiuse-column-manager__header {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  padding: 2px 4px 7px;\n}\n\n.whatiuse-column-manager__title { font-weight: 600; }\n\n.whatiuse-column-manager__header > span { color: var(--whatiuse-fg-subtle); }\n\n.whatiuse-column-manager__list { display: grid; gap: 2px; margin: 0; padding: 0; list-style: none; }\n\n.whatiuse-column-manager__row {\n  display: grid;\n  min-height: 36px;\n  grid-template-columns: minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 8px;\n  padding: 3px 3px 3px 7px;\n  border-radius: 7px;\n}\n\n.whatiuse-column-manager__row[data-pinned] { background: var(--whatiuse-selected); }\n\n.whatiuse-column-manager__row .whatiuse-choice-row { min-width: 0; }\n\n.whatiuse-column-manager__row .whatiuse-choice-copy { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n\n.whatiuse-column-manager__reset { width: 100%; justify-content: flex-start; margin-top: 5px; }\n\n@media (hover: hover) and (pointer: fine) {\n.whatiuse-column-manager__row:hover { background: var(--whatiuse-hover); }\n}\n}\n",
      "type": "registry:style",
      "target": "styles/components/column-manager.css"
    }
  ],
  "meta": {
    "whatiuse": {
      "contract": "/agent/whatiuse-agent.json",
      "registryItem": "@whatiuse/column-manager"
    }
  },
  "type": "registry:component"
}